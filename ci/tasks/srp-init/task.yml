---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: harbor.dhaka.cf-app.com/srp/srp-helper-task 
    username: ((srp-cli-registry-creds.username))
    password: ((srp-cli-registry-creds.password))

inputs:
- name: docs-platform-automation
- name: build-info

outputs:
- name: srp-data
params:
  CLIENT_ID:  
  CLIENT_SECRET:
  DOMAIN:

run:
  path: docs-platform-automation/ci/tasks/srp-init/task
