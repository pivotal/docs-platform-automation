---
platform: linux
image_resource:
  type: registry-image
  source:
    repository: harbor-repo.vmware.com/source_insight_tooling/osspi-runner

inputs:
  - name: docs-platform-automation
  - name: image
    optional: true
params:
  TAR_PATH:
  API_KEY:
  OSM_ENVIRONMENT: beta
  OSSTP_LOAD_DRY_RUN:
  ENDPOINT:
  USERNAME:
  PRODUCT:
  VERSION:
  OSSPI_IGNORE_RULES:
  APPEND:
  CT_TRACKER_OS: ubuntu
run:
  path: docs-platform-automation/ci/tasks/run-osspi-docker/task.sh
