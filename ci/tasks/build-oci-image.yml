---
platform: linux
image_resource:
  type: registry-image
  source:
    repository: ((concourse-team/dev_image_registry.url))/concourse/oci-build-task
    tag: 0.14.13
    username: ((concourse-team/dev_image_registry.username))                                           
    password: ((concourse-team/dev_image_registry.password))

inputs:
- name: docs-platform-automation
- name: base-image
# Used by Dockerfile.ci (om binary). Dockerfile.packages ignores this input.
- name: om-release
outputs:
- name: image
params:
  DOCKERFILE:
  IMAGE_ARG_base_image:
run:
  path: build
