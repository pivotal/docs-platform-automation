---
platform: linux
image_resource:
  type: registry-image
  source:
    repository: vito/oci-build-task

inputs:
- name: docs-platform-automation
outputs:
- name: image
params:
  DOCKERFILE:
  IMAGE_ARG_base_image:

run:
  path: build
